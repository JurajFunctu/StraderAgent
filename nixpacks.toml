[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install", "cd client && npm install && cd .."]

[phases.build]
cmds = ["cd client && npx vite build && cd ..", "npx tsc --project tsconfig.server.json || true"]

[start]
cmd = "node dist/index.js"
